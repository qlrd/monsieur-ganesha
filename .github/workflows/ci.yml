name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: Quality gates
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5
        with:
          version: "0.10.8"

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: uv sync --dev --frozen

      - name: Check formatting (black)
        run: uv run black --check src/ tests/

      - name: Check import order (isort)
        run: uv run isort --check-only src/ tests/

      - name: Lint (pylint)
        run: uv run pylint src/ganesha/

      - name: Run tests with coverage
        # fail_under = 80 is enforced via [tool.coverage.report] in
        # pyproject.toml; subprocess-only modules are omitted there.
        run: |
          uv run pytest \
            --cov=src/ganesha \
            --cov-report=term-missing \
            --cov-report=xml

      - name: Upload coverage report
        uses: codecov/codecov-action@671740ac38dd9b0130fbe1cec585b89eea48d3de  # v5
        if: always()
        with:
          files: coverage.xml
          fail_ci_if_error: false

  debian-stable-py310:
    name: Debian stable / py3.10 — pytest only
    runs-on: ubuntu-latest
    container: debian:stable

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Install Python and pip (APT)
        run: |
          apt-get update
          apt-get install -y python3 python3-pip python3-venv

      - name: Install package and test tools
        run: |
          python3 -m venv .venv
          .venv/bin/pip install -e . pytest norminette

      - name: Run tests
        run: .venv/bin/pytest -q
